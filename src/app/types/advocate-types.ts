// NOTE: Since this is now only used in one place, should this file be deleted?

export interface Advocate {
  firstName: string
  lastName: string
  city: string
  degree: string
  specialties: string[]
  yearsOfExperience: number
  phoneNumber: number
}